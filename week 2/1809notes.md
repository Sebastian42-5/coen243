int a = 2;
int b = 3;

a = a + b 
b = a - b 
a = a - b 

control statements 

:: = scope resolution operator  

if(condition) {
    doSomething();
} else {
    doSomethingElse();
}

cout << (grade >= 60 ? "passed" : "failed") << endl;

char has to be only one character, not multiple 

if(condition1)
{
    if(condition2){
        statement1;
    }
}
else
    statement2;

>, <, >=, <=, ==, !=, &&, ||, !